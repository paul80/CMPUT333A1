import argparse
import sys



parser=argparse.ArgumentParser()
parser.add_argument('start' ,nargs=1 , type=int)

print(((sys.argv)))

#def process_aguments(args):

 #   parser = argparse.ArgumentParser(description="insert arguments here for testin")

  #  parser.add_argments('-A', '--FEEDBACK',dest="Feedback",type = int, default = 0 )

   # parser.add_arguments('e', dest='error', type=float, default= .99)
    
    #options = parser.parse_args(args)
    #return vars(opts)
